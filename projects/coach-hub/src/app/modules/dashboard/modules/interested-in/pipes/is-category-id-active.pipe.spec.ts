import { IsCategoryIdActivePipe } from './is-category-id-active.pipe';

describe('IsCategoryIdActivePipe', () => {
  it('create an instance', () => {
    const pipe = new IsCategoryIdActivePipe();
    expect(pipe).toBeTruthy();
  });
});
